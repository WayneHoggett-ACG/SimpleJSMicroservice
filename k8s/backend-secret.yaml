apiVersion: v1
kind: Secret
metadata:
  name: backend-secrets
  labels:
    app: backend
    app.kubernetes.io/name: simple-grocery-store
    app.kubernetes.io/component: backend
type: Opaque
data:
  # Base64 encoded CosmosDB connection string
  # To encode: echo -n "your-cosmosdb-connection-string" | base64
  mongodb-uri: ""  # Add your base64 encoded CosmosDB connection string here
